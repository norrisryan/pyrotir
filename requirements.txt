# ROTIR JAX Requirements
# Python/JAX port of ROTIR for stellar surface reconstruction

# Core dependencies
jax[cpu]>=0.4.0  # or jax[cuda12] for GPU support
jaxlib>=0.4.0
numpy>=1.24.0
scipy>=1.10.0

# HEALPix and astronomy
healpy>=1.16.0  # For cross-validation of vertex computation
astropy>=5.0  # For OIFITS reading

# Optional for plotting
matplotlib>=3.7.0  # For plotting (optional, not core)

# Development dependencies
pytest>=7.0.0
